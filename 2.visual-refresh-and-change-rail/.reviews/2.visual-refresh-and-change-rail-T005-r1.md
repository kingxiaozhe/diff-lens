# T-005 审查凭证 r1 · 2026-07-18T00:56:34+0800 · codex exec --sandbox read-only
65,059
**Findings**

1. [tests/smoke.mjs](/Users/zero/MyCode/diff-lens/tests/smoke.mjs:160) 的 view-switch 断言能被“旧 rail DOM”骗绿。具体场景：如果回归导致切到 `split` 或切回 `unified` 时只更新了 `#result`，但没重建 `#pip-rail`，这里仍会通过。`#result .srow.hstart` 只证明 split 行渲染了；`#pip-rail .pip.length > 0` 在切回 unified 时又是个松条件，旧 pip 还在就立刻满足。这样 `splitTypes === "change,del,add"` 读到的其实可能还是 unified 阶段留下来的 class，测试会绿，但“view 切换会刷新 rail”这个真实路径没被测到。

2. [tests/smoke.mjs](/Users/zero/MyCode/diff-lens/tests/smoke.mjs:181) 把 viewport 恢复写死成 `1280x720`，这会让后续既有断言对本段顺序产生耦合。具体场景：如果前面或全局默认 viewport 以后改成别的尺寸来扩 responsive 覆盖，这段仍会把页面钉回 `1280x720`，后面的 `history menu stays inside the viewport` 之类几何断言就不再运行在它们原本的环境里，可能出现与本特性无关的通过/失败。

其余部分我没有看到新的具体假绿灯问题。特别是 [tests/smoke.mjs](/Users/zero/MyCode/diff-lens/tests/smoke.mjs:144) 这个 `=== 3` 等待是有效收紧；当前前序 move 用例是 2 个 hunk，新 rail 用例是 3 个 hunk，不是恒真条件。

结论：`changes requested`。本次是静态审查，未重跑套件。
