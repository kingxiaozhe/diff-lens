# T-003 审查凭证 r1 · 2026-07-18T00:48:42+0800 · codex exec --sandbox read-only
2. `changes requested` [app.js](/Users/zero/MyCode/diff-lens/app.js:197), [app.js](/Users/zero/MyCode/diff-lens/app.js:212), [app.js](/Users/zero/MyCode/diff-lens/app.js:353): `renderPips()` 在同一循环里交替读 `hunks[i].offsetTop`、写 `kids[i].style.top`。对大 diff（很多 hunks）来说，`wrap` 切换或 `resize` 后这会形成同步布局抖动，出现可见卡顿；新增的 `resize` 入口把这个成本从“内容重渲染时顺带发生”扩展到了“纯几何交互也会触发”。这是性能层面的具体失败场景。

除这两点外，我没看到你描述的那几个路径上有新的逻辑回归：`wrap` 后 top 重算、`.current`/计数器保持、三视图/折叠一致性，代码路径是自洽的。残余风险是 T-005 的自动化回归用例还没补上，所以这两个场景目前只能靠人工保证。
tokens used
38,938
1. `changes requested` [app.js](/Users/zero/MyCode/diff-lens/app.js:207), [app.js](/Users/zero/MyCode/diff-lens/app.js:348), [app.js](/Users/zero/MyCode/diff-lens/app.js:353): `renderPips()` 每次都用 `pipRail.innerHTML = ...` 整条重建轨道。现在 `wrap` 切换和 `resize` 都会走这条路径，所以如果用户当前键盘焦点停在某个 pip 上，随后缩窗/分屏或切 `wrap`，被聚焦的按钮会被销毁，`.current` 会恢复，但真实 DOM focus 不会恢复，键盘导航会丢位。这是新增几何刷新入口带出来的具体失败场景。

2. `changes requested` [app.js](/Users/zero/MyCode/diff-lens/app.js:197), [app.js](/Users/zero/MyCode/diff-lens/app.js:212), [app.js](/Users/zero/MyCode/diff-lens/app.js:353): `renderPips()` 在同一循环里交替读 `hunks[i].offsetTop`、写 `kids[i].style.top`。对大 diff（很多 hunks）来说，`wrap` 切换或 `resize` 后这会形成同步布局抖动，出现可见卡顿；新增的 `resize` 入口把这个成本从“内容重渲染时顺带发生”扩展到了“纯几何交互也会触发”。这是性能层面的具体失败场景。

除这两点外，我没看到你描述的那几个路径上有新的逻辑回归：`wrap` 后 top 重算、`.current`/计数器保持、三视图/折叠一致性，代码路径是自洽的。残余风险是 T-005 的自动化回归用例还没补上，所以这两个场景目前只能靠人工保证。
