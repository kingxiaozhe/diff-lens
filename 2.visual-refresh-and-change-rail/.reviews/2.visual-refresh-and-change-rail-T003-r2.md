# T-003 审查凭证 r2 · 2026-07-18T00:51:43+0800 · codex exec --sandbox read-only
零发现。静态复核 [app.js](/Users/zero/MyCode/diff-lens/app.js:197) 到 [app.js](/Users/zero/MyCode/diff-lens/app.js:222) 后，这两处修复都已到位，且我没看到新的逻辑回归：

- 焦点问题已闭合：重建前记录 `document.activeElement` 的 `data-pip`，重建后用 `focus({ preventScroll: true })` 还原到同序号 pip，且有 `kids[focusIdx]` 守卫，不会越界报错。
- 布局抖动问题已闭合：先批量读取 `offsetTop` 生成 `tops`，再统一写回 `style.top`，不再有逐项读写交错导致的强制同步布局。

验证上，`node tests/diff-test.mjs` 通过（`48 passed, 0 failed`），`node tests/history-test.mjs` 通过（`19 passed, 0 failed`）。残余风险只有两点：`tests/smoke.mjs` 在当前只读沙箱里因 Playwright `mkdtemp` 权限失败未能复跑；另外 AC-013/AC-014 对应的 pip 烟测断言目前看起来仍未补进 `tests/smoke.mjs`。结论：这轮 `renderPips` 修复我这里给 `approved`。
