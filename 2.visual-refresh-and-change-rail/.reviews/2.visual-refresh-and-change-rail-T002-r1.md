# T-002 审查凭证 r1 · 2026-07-18T00:37:16+0800 · codex exec --sandbox read-only
2.visual-refresh-and-change-rail/requirements.md:43:- 安全: 零网络、零新权限；pip 内容仅含引擎数据（类型 + 行号），不插值用户文本
2.visual-refresh-and-change-rail/requirements.md:49:- [ ] [AC-002] 存在差异时轨道可见，pip 数量 === 结果区 `.hstart` 数量（smoke 断言）
2.visual-refresh-and-change-rail/requirements.md:50:- [ ] [AC-003] 点击第 N 个 pip → 第 N 个 hunk 滚入视口且带 `.jumped` 描边、`#nav-count` 显示 `N / 总数`（smoke 真实点击路径断言）
2.visual-refresh-and-change-rail/requirements.md:52:- [ ] [AC-005] pip 颜色取自 `:root` 语义色变量（绿/红/琥珀/强调），不引用任何框架角色色
2.visual-refresh-and-change-rail/requirements.md:53:- [ ] [AC-006] 每个 pip 为真实 `<button type="button">`，带 `title` + `aria-label`，Tab 可聚焦且有可见焦点环
2.visual-refresh-and-change-rail/requirements.md:59:- [ ] [AC-012] **Split 视图**下 pip 仍带正确类型类与行号（smoke 断言——防「只在 Unified 工作」）
2.visual-refresh-and-change-rail/requirements.md:60:- [ ] [AC-013] 跳转到第 N 处后切换 Word wrap，`.current` 仍在第 N 个 pip 上且位置已重算（smoke 断言）
2.visual-refresh-and-change-rail/requirements.md:61:- [ ] [AC-014] 视口 1440→620 缩放后 pip `style.top` 集合发生重算（smoke `setViewportSize` 断言——防 resize 位置漂移）

exec
/bin/zsh -lc "rg -n \"function render\\(|optWrap\\.addEventListener|window\\.addEventListener\\(\\s*\\\"resize|window\\.addEventListener\\(\\s*'resize|renderPips\\(\" app.js" in /Users/zero/MyCode/diff-lens
 succeeded in 0ms:
131:  function render() {
182:    renderPips();
194:  function renderPips() {
347:  optWrap.addEventListener("change", () => { result.classList.toggle("wrap", optWrap.checked); persistOpts(); });

codex
1. 中严重度: `.current` 的“幂等复原”在实际重建路径上失效。`[app.js](/Users/zero/MyCode/diff-lens/app.js:178)` 的 `indexHunks()` 每次重建都先把 `hunkIdx = -1`，所以 `[app.js](/Users/zero/MyCode/diff-lens/app.js:214)` 里 `renderPips()` 末尾的“复原 `.current`”分支根本走不到。复现：准备至少 2 个 hunk，点击第 2 个 pip，使其高亮；然后切换 `Unified -> Inline` / `Split`，或在折叠模式下点一次 `foldbtn` 触发重渲染。结果是 pip 高亮丢失，`#nav-count` 也从 `2 / N` 退回成 `N diffs`，用户当前定位状态被清空。

2. 中严重度: 几何变化后 pip 位置不会重算，轨道会漂移。当前 `renderPips()` 只在重渲染时调用一次 `[app.js](/Users/zero/MyCode/diff-lens/app.js:182)`；但 `Word wrap` 只是切类名 `[app.js](/Users/zero/MyCode/diff-lens/app.js:347)`，没有重新计算 `style.top`，而且代码里也没有 `resize` 监听。复现：输入含长行的 diff，让若干 hunk 在 wrap 开/关时明显换行；先记住 pip 分布，再切 `Word wrap`，或把窗口从宽拖窄。结果是正文行的 `offsetTop` 已变，但 pip 还停在旧百分比位置，轨道与实际改动分布不再对应。

未看到这次新增代码里有“用户文本进入 pip HTML/title/aria-label”的路径，因此没发现可由输入触发的 XSS 点；事件委托、视图无关元数据、语义色变量和双主题这几项实现本身看起来正常。测试我没有复跑。
tokens used
52,867
1. 中严重度: `.current` 的“幂等复原”在实际重建路径上失效。`[app.js](/Users/zero/MyCode/diff-lens/app.js:178)` 的 `indexHunks()` 每次重建都先把 `hunkIdx = -1`，所以 `[app.js](/Users/zero/MyCode/diff-lens/app.js:214)` 里 `renderPips()` 末尾的“复原 `.current`”分支根本走不到。复现：准备至少 2 个 hunk，点击第 2 个 pip，使其高亮；然后切换 `Unified -> Inline` / `Split`，或在折叠模式下点一次 `foldbtn` 触发重渲染。结果是 pip 高亮丢失，`#nav-count` 也从 `2 / N` 退回成 `N diffs`，用户当前定位状态被清空。

2. 中严重度: 几何变化后 pip 位置不会重算，轨道会漂移。当前 `renderPips()` 只在重渲染时调用一次 `[app.js](/Users/zero/MyCode/diff-lens/app.js:182)`；但 `Word wrap` 只是切类名 `[app.js](/Users/zero/MyCode/diff-lens/app.js:347)`，没有重新计算 `style.top`，而且代码里也没有 `resize` 监听。复现：输入含长行的 diff，让若干 hunk 在 wrap 开/关时明显换行；先记住 pip 分布，再切 `Word wrap`，或把窗口从宽拖窄。结果是正文行的 `offsetTop` 已变，但 pip 还停在旧百分比位置，轨道与实际改动分布不再对应。

未看到这次新增代码里有“用户文本进入 pip HTML/title/aria-label”的路径，因此没发现可由输入触发的 XSS 点；事件委托、视图无关元数据、语义色变量和双主题这几项实现本身看起来正常。测试我没有复跑。
