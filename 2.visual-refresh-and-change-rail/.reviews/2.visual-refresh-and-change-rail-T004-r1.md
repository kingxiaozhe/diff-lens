# T-004 审查凭证 r1 · 2026-07-18T00:43:00+0800 · codex exec --sandbox read-only
其余你点名的硬约束，我没看到具体失败场景：13px/12px 密度没变，语义色没有改映射，`.pip-rail` 样式段未被触碰，新增滚动条颜色都引用既有变量，`@media (prefers-reduced-motion: reduce)` 仍会全局关掉这次新增的过渡。滚动条这组 `scrollbar-color + ::-webkit-scrollbar*` 也没看到确定会坏的兼容性坑，最多是旧浏览器回退到系统滚动条。  

结论：`changes requested`。
tokens used
30,054
1. `app.css:132` 仍有裸 `border-radius: 7px` 在 `.mark` 上。若 T-004 的验收是“控件/容器圆角收敛到 `--r-ctl` / `--r-box` 两级 token”，这次改动没有闭合约束：当前文件里仍然同时存在 token 化圆角和原始 `7px`。具体失败场景是设计验收或后续主题调整时，品牌方块不会跟随 token 一起收敛，导致页面上继续保留第三档圆角。

2. `app.css:200-203` 这组选择器声明了“控件统一的过渡与按压反馈”，但 `:active` 实际只给了 `.action/.navbtn/.swap/.segbtn`。同文件里的 `.menu > summary`、`.item`、`.hist-restore`、`.hist-del`、`.foldbtn`（见 `app.css:320-360, 448-453`）都有过渡却没有按压态。具体失败场景是用户点击历史项、删除按钮、折叠按钮或菜单触发器时，不会出现同一套按压反馈，交互一致性验收会失败。

其余你点名的硬约束，我没看到具体失败场景：13px/12px 密度没变，语义色没有改映射，`.pip-rail` 样式段未被触碰，新增滚动条颜色都引用既有变量，`@media (prefers-reduced-motion: reduce)` 仍会全局关掉这次新增的过渡。滚动条这组 `scrollbar-color + ::-webkit-scrollbar*` 也没看到确定会坏的兼容性坑，最多是旧浏览器回退到系统滚动条。  

结论：`changes requested`。
