# T-002 + T-003（compare.html / app.css / app.js）— 审查凭证 r1

- 时间: 2026-07-17T13:12:42Z
- 调用方式: **对抗子 agent（降级链第 2 级）**；降级原因原文: `command -v codex` 无输出（Codex 未安装）
- 提示词: "假设这段 diff 有害，找出证据"（全新上下文）
- **刻意偏离**: N4 要求按单 task 粒度，此处合并 T-002+T-003。理由: 已知的失败模式（`.histlist` 类名一边改一边没改）是跨文件的，拆开审等于让审查员看不见另一半。
- 结论: **不是零发现 —— 6 条，2 条是本次重做引入的真回归**

## 会退化（真回归）
1. **textarea resize 把手能把结果区压没且无滚动条**（app.css .app）— `height:100vh` + `minmax(0,1fr)`。实测拖到 700px → 结果区 30px（1 行）；900px → 2px（0 行），页面不滚。旧版结果区恒 74px 且页面正常滚动。**已复现属实**。
2. **≤900px 堆叠时结果区被吃掉一半以上**（同根因）— 880px 20/31 行 vs 旧版 26/31；620px 12/31 vs 旧版 19/31。「全宽是净胜」只在宽屏成立。
3. **a11y: 分组后无障碍名丢了 "Ignore" 语义**（compare.html .group）— 屏幕阅读器读 "Whitespace"（旧版读 "Ignore whitespace"），且同屏还有 "Show whitespace"，两者听起来分不出谁是忽略谁是显示。

## 小瑕疵
4. `<500px` 出现横向滚动（旧版下限 440px）— R-008 说"任何宽度不得出现"。
5. 三处 tooltip 在重做中丢失（#opt-fold / #fmt-json 的 key-order 说明 / #history / #export）。
6. 暗色选中视图按钮白字对比度 2.56:1（NFR 要求 ≥4.5）。旧版 ~2.2，非回归，但规则这次被重写且它是"当前视图"的唯一视觉载体。

## 明确的零发现（穷举而非抽样）
- **跨文件三方对齐**: CSSOM 递归枚举全部选择器 × 13 个 DOM 状态做集合比对 —— **无第二处 .histlist 式失配**，死规则 0，裸奔 class 仅 `hstart`/`split` 两个纯 JS 钩子（旧版本来就无规则）。`.scol.b` 修复实测生效。
- **XSS**: 5 种 payload × 6 条渲染路径 —— 注入节点 0、执行 0。
- **功能回归**: 26 个控件 id 全在；三条测试全绿；storage 9 键与 onChanged 实测覆盖。
- **语义色**: 亮 L 极差 0.0020 / 暗 0.0011；正文对比度 亮 13.22–13.71 / 暗 10.98–11.56。
- **响应式**: 9 档视口，7 个开关全可见、无重叠。

## 处置：6/6 全部采纳并修复，逐条实测复验
| 发现 | 修法 | 复验 |
| ---- | ---- | ---- |
| 1 | `minmax(160px, 1fr)` 给结果区下限 | 最坏 7 行可见 + 页面滚动（旧版 4 行） |
| 2 | 堆叠断点 900px → **480px**（真凶是堆叠让两个输入框各收一次垂直税，不是输入框高度） | **1440=28 / 880=27 / 620=18**（旧版 26/26/19）—— 全面持平或反超 |
| 3 | `role="group"` + `aria-labelledby` + 每个 input 补 aria-label | 实测读出 "Ignore whitespace" 等 |
| 4 | 根因是 grid 隐式 `auto` 列的最小值取 min-content（我修了行轴 min-height:0 却漏了列轴）+ `.group` 内部不换行 | **320px 起全部无横滚**，优于旧版 440px |
| 5 | 三处 tooltip 全部恢复 | 实测 title 属性存在 |
| 6 | 暗色 `.segbtn.on { color: #10131c }` | 亮 6.37 / 暗 **7.26** |
