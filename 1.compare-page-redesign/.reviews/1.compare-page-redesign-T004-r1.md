# T-004 (tests/smoke.mjs) — 审查凭证 r1

- 时间: 2026-07-17T05:22:08Z
- 调用方式: **对抗子 agent（降级链第 2 级）**
- 降级原因原文: `command -v codex` 无输出 —— Codex 未安装。npm global 里的 `oh-my-codex` 是无关包。
- 提示词: "假设这段 diff 有害，找出证据"（全新上下文）
- 结论: **不是零发现 —— 9 条，4 条会炸**。审查员实际运行了变异测试，每条附实测。

## 会炸
1. **注释过滤器是绕过通道**（:281）— `.filter(l => !/^\s*(\/\/|\*|\/\*)/)` 丢弃**整行**。实测 `/* harmless */ var X = "https://evil.example/collect";` → 46/0 全绿。且 `* { background: url(//evil/x.png) }` 在 CSS 里 `*` 是合法选择器 → 既是绕过也是误杀。
2. **协议相对 URL 漏网**（:277）— `<script src="//cdn.jsdelivr.net/npm/tracker.js">` → 46/0 全绿。
3. **拼接 URL 漏网，且"抓到"是假象**（:277）— `fetch("//evil/collect")` 时 "app.js makes no external requests" 断言**是 PASS 的**；变红只因沙箱无网 fetch 抛错被 pageerror 捕获。**真实带网环境全绿**。
4. **守卫排在最后且无隔离**（:180-285）— storage 块超时崩溃 → manifest 守卫与外链守卫**一行未跑**，汇总行未打印，exit 1 但无人知道最重要的不变式这轮没验。

## 会退化
5. AC-013 的"水平居中"采集了从未断言（:140 vs :142）— 去掉 place-items:center → 46/0 绿。
6. AC-015 的 9 个键只覆盖 5 个（:234-244）— opt-ws/opt-blank/opt-char/opt-ws-show 未测，history 键未测。**按 AC-015 字面要求，这轮不该判通过**。
7. manifest 守卫漏 4 条扩权路径（:261-272）— web_accessible_resources / externally_connectable / declarative_net_request / **content_security_policy 放宽 connect-src**（恰是它声称要挡的外泄手段）。
8. onChanged 的 areaName 守卫测不到（:215）— stub 恒传 "local"。

## 小瑕疵
9. `ok(true, ...)` 恒真（:250）；第二个 browser 无句柄，异常时泄漏。

## 明确的非发现（未凑数）
- waitForTimeout(200) 未能复现 flake（多次连跑全绿），是味道但拿不出失败场景。
- 测试不污染仓库（干净 worktree 验证）。
- M1/M2/M5/N1/N5/U2/U3/S1 变异均正确变红 —— 守卫对**直接**扩权与外链确实有效。

**审查员总体判断**: 权限守卫对直接扩权真有效（比文档撒谎两个版本强得多），但按 AC-015/AC-016 字面要求**这轮不该判通过**。

---

## 处置：9/9 全部采纳并修复（2026-07-17T13:12:42Z）

| 发现 | 修法 | 变异复验 |
| ---- | ---- | ---- |
| 1 注释过滤器是绕过通道 | 改为先剥离注释再匹配（`/*...*/` 与行内 `//`） | `/* harmless */ var X="https://evil…"` → ✓ 拦截 |
| 2 协议相对 URL 漏网 | `["'(]\s*//[a-z0-9-]` —— 只在 URL 能出现的位置匹配（裸 `//` 会误杀每个行尾注释，我first踩了这个） | `<script src="//cdn…">` → ✓ 拦截；CSS `url(//…)` → ✓ 拦截 |
| 3 拼接 URL 正则永远追不上 | **新增 `page.route("**")` 运行时拦截**，断言零对外请求 —— 正则降为第一道粗筛 | 基线 ✓；正则无法覆盖的路径由观测兜底 |
| 4 **守卫排最后、上游一崩就整段跳过** | **提到文件最前**，浏览器启动之前，不依赖任何东西 | 破坏 onChanged 致崩 + 偷加 tabs → ✓ 权限守卫**照样报警**，汇总行照常打印 |
| 5 AC-013 居中采集了没断言 | 补 `ok(place.includes("center"))` | 去掉 place-items → ✓ 拦截 |
| 6 AC-015 9 键只覆盖 5 个 | 遍历全部 6 个 toggle + wrap + view + history | 漏回填 ignoreWhitespace / charLevel → ✓ 分别拦截 |
| 7 manifest 守卫漏 4 条扩权路径 | 补 web_accessible_resources / externally_connectable / declarative_net_request / sandbox / **CSP 外部 origin** | 四条 → ✓ 全部拦截 |
| 8 areaName 守卫测不到 | stub 加 `__fireArea`，断言 sync 区写入被忽略 | 去掉 areaName 守卫 → ✓ 拦截 |
| 9 `ok(true)` 恒真 | 改为轮询后断言实际值 | — |

**变异测试总计 13 项，全部拦截。** 断言数 46 → **57**。
