# package.mjs — 审查凭证 r1

- 时间: 2026-07-17T05:22:08Z
- 调用方式: **对抗子 agent（降级链第 2 级）**；降级原因: Codex 未安装
- 结论: **不是零发现 —— 22 个变异测试跑出 16 条**

## 会炸（静默产出坏包，exit 0，校验环节亲手放行）
1. **F1 正则只认双引号** → `src='app.js'` 时主脚本从 zip 静默消失（10 files, exit 0）。**已复核属实**。
2. **F2 MV3 一半引用字段没读** → default_locale / sandbox / chrome_url_overrides / side_panel / declarative_net_request / storage.managed_schema 六个静默漏打。default_locale 会让 Chrome **拒绝安装**。
3. **F3 `../` 直接打进 zip** → `src="../outside-secret.js"` 把仓库外文件**连内容**打进上架包，exit 0，校验通过。**已复核属实**（实测泄露 "SECRET-DO-NOT-SHIP"）。「按构造不可能污染」被证伪。
4. F4 CSS 的 url() 不追（埋雷：今天 app.css 恰好没有 url()）。
5. F5 ROOTS 就是它声称自己没有的手工白名单。
6. F6 img srcset / 内联 style url() / link rel=manifest 全漏。

## 会退化（好包被拒）
F7 default_icon 写成字符串 → Object.values 拆成单字符（**已复核属实**）；F8 web_accessible_resources 的 `icons/*` 通配；F9 根相对路径 `/app.css`；F10 注释掉的 script 标签；F11 percent-encoded 路径；F12 非 ASCII 文件名（本仓库已有 运行日志.jsonl）导致假失配。

## 小瑕疵
F13 macOS 大小写不敏感 → Mac 绿 / Linux 红；F14 unzip 缺失时**留下未校验的同名 zip**；F15 **校验是同义反复**（22 变异只抓到 1 个，还制造 1 个假阳性）；F16 完全不管权限/外链硬约束。

**审查员总体判断**: "这个脚本的正确名字不是「推导」，是「对一种恰好在今天成立的写法的模式匹配」"。

## 处置
**全部采纳，推倒重写**。不再用正则解析 HTML + 枚举 MV3 字段（输不完的游戏），改为：
- 显式 PAYLOAD 列表（11 项）→ F1/F3/F4/F6/F7/F8/F9/F10/F11 消失（不再解析）
- 递归扫 manifest 里路径形状的字符串 → F2 修复（实测拦住 _locales / sandbox / declarative_net_request）
- 路径逃逸校验 → F3 兜底
- 打包集上强制权限白名单 + 外链扫描 → F16 修复
- **解压 zip 跑真实 smoke 套件** → 这是唯一真正的完整性证明，取代同义反复的校验（F15）
- staging 名构建、校验通过才 rename → F14 修复
